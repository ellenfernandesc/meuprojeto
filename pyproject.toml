[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "studyorganizer"
version = "0.2.0"
description = "Organizador de Estudos Eficiente"
dependencies = [
    "requests",
]
